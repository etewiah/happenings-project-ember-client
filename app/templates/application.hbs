<div>
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container-fluid">
        <div class="brand">
          Happenings Project Example Client
        </div>

        <ul class="nav pull-right">
                      <li class="divider-vertical"></li>
                      <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{ navStructure.city }} <b class="caret"></b></a>
                          <ul class="dropdown-menu">
                          {{#each item in cityNavItems}}
                            <li ><a {{action 'filterHappeningsByCity' cityNavItems item}}> {{item.title}} </a></li>
                          {{/each}}
                          </ul>
           
                      </li>
                    </ul>


      </div>
    </div>
  </div>
  <div class="container-fluid" id="main">
    {{ outlet }}
  </div>
</div>
